# ChordAI — יומן בנייה והחלטות

מסמך זה נכתב תוך כדי הבנייה ומרכז את ההחלטות החשובות, המלכודות שנתקלנו בהן,
והדברים שצריך לזכור בהמשך.

## מה התוכנה עושה

1. מקבלת קישור יוטיוב **או** הקלטה/העלאה של קובץ אודיו.
2. מזהה אקורדים מתוך האודיו.
3. מתמללת את המילים עם תזמון ברמת המילה הבודדת.
4. מחברת בין השניים — כל אקורד יושב מעל המילה שבה הוא מתנגן.
5. מציגה: נגן עם הדגשה בזמן אמת, תצוגת מובייל, וגרסת הדפסה.

## מבנה הפרויקט

```
chordai/
├── analyzer/      פייתון — זיהוי אקורדים + תמלול
├── server/        Node/Express — API, הורדות, ניהול משימות
├── web/           React + Vite — הממשק
├── data/          אחסון: audio/ (קבצי שמע), songs/ (JSON של כל שיר)
└── .venv/         סביבת פייתון מבודדת
```

## החלטות טכניות מרכזיות

### למה פייתון לניתוח ו-Node לשרת
זיהוי אקורדים ותמלול דורשים את `librosa` ו-`faster-whisper` שקיימים רק בפייתון.
השרת מריץ את הפייתון כתת-תהליך וקורא ממנו שורות JSON כדי לדווח התקדמות בזמן אמת.

### זיהוי אקורדים (`analyzer/chords.py`)
הצינור: הפרדת רכיב הרמוני → כרומה מבוססת CQT → סנכרון לפי פעימות → התאמה
לתבניות אקורדים → פענוח Viterbi.

- **הפרדה הרמונית לפני הכל.** כלי הקצב מפזרים אנרגיה על כל 12 הצלילים ומשבשים
  את הזיהוי. `librosa.effects.harmonic` מנקה את זה.
- **סנכרון לפי פעימות ולא לפי פריימים.** אקורדים מתחלפים על פעימות, וזה גם מוזיל
  את חישוב ה-Viterbi בסדר גודל.
- **התבניות מנורמלות ל-L2.** בלי זה אקורדים בני 4 צלילים (7, maj7) היו מנצחים
  תמיד משולשים פשוטים, כי יש להם יותר אנרגיה. בנוסף יש משקל שמעדיף אקורדים פשוטים.
- **השורש והקווינטה מקבלים דגש** (1.35 ו-1.10) — הם מזהים את האקורד, וטרצה חלשה
  לא אמורה להפוך מז'ור למינור.
- **Viterbi עם הסתברות היצמדות 0.55** — בלי זה האקורדים "מרצדים" בין פעימות.
- **מיזוג מקטעים קצרים מ-0.45 שניות** — אלה כמעט תמיד תקלות זיהוי ולא נגינה אמיתית.

### 🔴 מלכודת שנפלנו בה: היסט של פעימה אחת
`librosa.util.sync` מחזיר **מספר פעימות + 1** עמודות — יש עמודה מובילה שמכסה את
האודיו שלפני הפעימה הראשונה. במימוש הראשון מיפינו עמודה i לזמן של פעימה i, וזה
"בלע" את כל תחילת השיר (1.86 שניות בבדיקה) והזיז את כל האקורדים.
**התיקון:** גבולות הזמן חייבים להיות `[0.0, *זמני_הפעימות, משך_השיר]`.
אם אי פעם משנים את שלב הסנכרון — לבדוק את זה שוב.

### תמלול (`analyzer/lyrics.py`)
`faster-whisper` עם `compute_type="int8"` על CPU. על M4 Pro זה מהיר מאוד ולא דורש
הגדרות GPU.
- **`vad_filter=True` הוא קריטי לשירים.** בלי זיהוי פעילות קולית, ויספר "ממציא"
  מילים על פני קטעים אינסטרומנטליים (פתיחים, סולואים).
- **`condition_on_previous_text=False`** מונע לולאות של חזרה על אותה שורה.
- **`word_timestamps=True`** — זה מה שמאפשר למקם אקורד מעל ההברה המדויקת.

### 🔴 שמות משתמשים ועברית
המודל `medium` הוא ברירת המחדל. לעברית `large-v3` נותן תוצאות ניכרות יותר טובות
אבל ההורדה כבדה (~1.5GB) והריצה איטית יותר. ניתן לשנות ב-`.env`.

## זיכרון שירים — לא לנתח פעמיים את אותו דבר

ניתוח שיר עולה דקות. לכן לפני שנפתחת משימה חדשה המערכת בודקת אם השיר כבר קיים:

- **יוטיוב** — לפי `videoId`, לא לפי הקישור המלא. כך `youtu.be/XXX` ו-
  `youtube.com/watch?v=XXX` מזוהים כאותו שיר.
- **העלאה/הקלטה** — לפי **גיבוב SHA-256 של תוכן הקובץ** (`audioHash`). אותו קובץ
  בדיוק יזוהה גם אם שינו לו את השם.

במקרה של התאמה השרת מחזיר `{songId, cached:true}` והדפדפן קופץ ישר לשיר —
נמדד 17ms במקום דקות. יש תיבת סימון "לנתח מחדש" ששולחת `force:true` ועוקפת את זה
(שימושי כשרוצים לנתח שוב עם מודל תמלול מדויק יותר).

⚠️ שירים שנותחו לפני השינוי הזה נשמרו בלי `audioHash`, ולכן העלאה חוזרת שלהם
תיצור כפילות. שירי יוטיוב ישנים מזוהים כרגיל כי `videoId` תמיד נשמר.

## ניקוי קבצים יתומים
משימה שנכשלת באמצע משאירה קובץ שמע בלי רשומת שיר. נמצאו ונמחקו 3 כאלה (16MB).
כדאי לחזור על הבדיקה הזו מדי פעם — משווים את `data/audio/` מול שדה `audioFile`
בכל קובץ ב-`data/songs/`.

## הפרדת קול מכלים (`analyzer/separate.py`)

Demucs מפריד את השיר לשני ערוצים: שירה, וכל השאר. אחר כך:
- **התמלול** רץ על ערוץ השירה בלבד — ויספר לא נאבק בתזמורת.
- **זיהוי האקורדים** רץ על ערוץ הכלים בלבד — מנגינת הזמר כבר לא מתפרשת כהרמוניה.

נמדד: שיר של 5.7 דקות, הפרדה + זיהוי אקורדים = 41 שניות (עם MPS).

הגנות שחייבות להישאר:
- **נפילה חזרה ל-CPU** אם MPS נכשל, ואז למקור אם ההפרדה נכשלה לגמרי. ההפרדה היא
  שיפור, לא תנאי הכרחי.
- **בדיקת אנרגיה בערוץ הכלים** (`_has_content`). בהקלטה של שירה ללא ליווי הערוץ
  הזה כמעט ריק, ואז חייבים לזהות אקורדים מהמקור.

### 🔴 מלכודת: ההפרדה שברה את זיהוי הסולם
זיהוי הסולם עבד בהתאמת פרופיל כרומה. ברגע שהסרנו את השירה, נעלמה מנגינת הזמר
שמעידה על הטוניקה, ושיר בבירור בסולם רה מינור זוהה כ**רה מז'ור**.
**התיקון:** הסולם נגזר עכשיו מרצף האקורדים שכבר זוהה (`detect_key_from_chords`) —
נבדק לפי כמה זמן כל אקורד מתנגן ואיזו דרגה הוא בסולם. זה עמיד הרבה יותר, ולא מושפע
כלל מהשאלה אם השירה נמצאת באודיו. שיטת הכרומה נשארה כגיבוי בלבד.

## מילים מדויקות — הדבקה וסנכרון (`analyzer/resync.py`)

תמלול אוטומטי של שירה בעברית לא יהיה מדויק תמיד, ובטח לא בשירים ליטורגיים.
הפתרון: המשתמש מדביק את המילים האמיתיות, ואנחנו **שומרים את המילים שלו** ולוקחים
רק את **התזמון** מהתמלול.

האלגוריתם: יישור Needleman-Wunsch בין רצף המילים המודבק לרצף המילים המתומלל.
- ההשוואה על טקסט מנורמל: בלי ניקוד, בלי פיסוק, ואותיות סופיות מומרות לרגילות.
- מילה שהותאמה מקבלת את הזמן של המילה המקבילה בתמלול.
- רצף מילים שלא הותאם מקבל זמנים באינטרפולציה בין שני העוגנים שמסביבו.
- שורות ריקות בטקסט המודבק הופכות למעברי בתים.

נבדק: תמלול שגוי ("לכארץ", "הרוך", "שירי אהבה") מול המילים הנכונות — כל השגיאות
תוקנו, והשורה שהתמלול בכלל לא שמע קיבלה תזמון סביר באקסטרפולציה.

### 🔴 מלכודת: אקסטרפולציה בקצה
מילים שאחרי העוגן האחרון קיבלו 0.45 שניות למילה באופן שרירותי. בשיר שבו התמלול כיסה
רק 60 שניות מתוך 258, כל שאר המילים נדחסו לשלוש שניות ולא קיבלו אקורדים כלל.
**התיקון:** האקסטרפולציה נמתחת עד לסוף האזור שבו באמת יש שירה (זמן המילה האחרונה
בתמלול), עם תקרה של 2.5 שניות למילה כדי שמילה בודדת לא תימתח על חצי שיר.

## עריכה ידנית בדף

לחיצה על מילה או אקורד במצב עריכה פותחת שדה קלט במקום. השמירה אוטומטית, בהשהיה של
0.7 שניות אחרי ההקלדה האחרונה.

**אקורד שנערך נשמר כצליל המושמע, לא כמו שהוא מוצג.** אם המשתמש בטרנספוזיציה או עם
קאפו, מה שהוא הקליד מומר חזרה (`toStoredChord`) לפני השמירה. אחרת הטרנספוזיציה
הייתה "נצרבת" לתוך הנתונים והופכת מהגדרת תצוגה לשינוי קבוע.

### 🔴 מלכודת: תופעות לוואי בתוך פונקציית עדכון של React
המימוש הראשון תיזמן את השמירה **בתוך** `setSong(previous => {...})`. התוצאה:
העריכה הופיעה על המסך אבל **מעולם לא נשמרה לשרת** — בלי שום הודעת שגיאה.
React לא מבטיח שתופעות לוואי בתוך פונקציית עדכון ירוצו, או ירוצו פעם אחת בלבד.
**הכלל:** פונקציית העדכון חייבת להיות טהורה. מחשבים את המצב החדש בחוץ, קוראים
ל-`setSong(next)`, ורק אז מתזמנים את השמירה.

### רשימת האקורדים חייבת להיגזר מהמילים
`song.blocks[].chords` הוא צילום מצב מרגע הניתוח. אחרי עריכה ידנית הוא מתיישן,
ולכן `uniqueChords` סורק את `word.chords` ואת `chordsBefore` ישירות.

## ביצועים ומה באמת עולה זמן

מדידה על שיר של 4:18 (M4 Pro):

| שלב | לפני | אחרי |
| --- | --- | --- |
| הפרדת קול (Demucs/MPS) | 27s | 27s |
| זיהוי אקורדים | 10s | רץ במקביל |
| תמלול large-v3 | 24s | 36s (VAD מרוכך) |
| **סה"כ** | **61s** | **58s** |

טעינת מודל התמלול היא 2.2 שניות בלבד — **אין טעם בשרת פייתון מתמיד**. זו הייתה
ההשערה הראשונה שלי והמדידה הפריכה אותה.

**זיהוי האקורדים והתמלול רצים במקביל** (`ThreadPoolExecutor`). הם קוראים ערוצים
שונים ולא חולקים מצב, ושניהם מבלים את הזמן בקוד נייטיב ששחרר את ה-GIL — נמדד 303%
ניצול מעבד. זה מה שקיזז את הזמן הנוסף של התמלול המדויק יותר.

### 🔴 מלכודת: ה-VAD זרק את רוב המילים
`vad_filter` נועד למנוע מוויספר "להמציא" מילים על קטעי נגינה. אבל **אחרי ההפרדה
אין מוזיקה להזות עליה**, וה-VAD הקשיח פשוט חתך שירה אמיתית. נמדד על אותו שיר:

| הגדרה | מילים | זמן |
| --- | --- | --- |
| VAD קשיח (המקורי) | 20 | 19s |
| VAD מרוכך | 46 | 36s |
| בלי VAD בכלל | 64 | 84s |

**הכלל שנקבע:** חומרת ה-VAD נגזרת ממה שמזינים. ערוץ שירה נקי → מרוכך
(`min_silence_duration_ms=2000, threshold=0.25`). מיקס מלא → קשיח, כי שם ההגנה
עדיין נחוצה. בשיר המבחן זה הכפיל את מספר המילים והרחיב את הכיסוי מ-60 ל-217 שניות.

## מודל הנתונים — שימו לב
- כל מילה מחזיקה **רשימת אקורדים** (`word.chords`), לא אקורד בודד. נדרש כי צליל
  מוחזק יכול להימשך על פני חילוף אקורד, ואסור לאבד את השני.
- `song.asrWords` שומר את התמלול המקורי כדי שאפשר יהיה להדביק מילים ולסנכרן
  שוב ושוב בלי להריץ תמלול מחדש. שירים שנותחו לפני שהשדה הזה נוסף נופלים חזרה
  לזמנים שכבר שמורים בתוך הבלוקים.
- `assemble_blocks` מכבד `stanzaBreak` שכבר סומן (שורה ריקה שהמשתמש הדביק) ורק
  אחר כך מוסיף מעברים לפי פערי זמן.

## 🔴 עברית ו-RTL — שני דברים שחוזרים
1. **שמות אקורדים הם לטיניים ובתוך דף RTL הם מתהפכים.** "A#" מוצג כ-"#A" אם אין
   `direction: ltr; unicode-bidi: isolate`. קרה פעמיים: פעם בגיליון עצמו ופעם
   ברשימת "אקורדים בשיר". בכל מקום חדש שמציג שם אקורד — לוודא שיש את הכלל הזה.
2. **פס ההתקדמות של הנגן** חייב `direction: ltr` בנפרד, אחרת הוא רץ הפוך.

## מובייל — הכותרת בלעה את המסך
בתצוגת טלפון הכותרת, התגיות, רשימת האקורדים וסרגל הכלים מילאו את כל המסך הראשון,
והמילים — שהן כל העניין — התחילו רק אחרי גלילה. התיקון: מתחת ל-620px סרגל הכלים
ורשימת האקורדים הופכים לשורה אחת עם גלילה אופקית במקום להישבר לארבע שורות.

## בדיקת שפיות שעברה
אודיו סינתטי של C → G → Am → F (2.4 שניות לאקורד) זוהה נכון לחלוטין,
כולל סולם (C major) וטמפו (103 מול 100 אמיתי).
הקובץ נוצר ידנית לבדיקה — אם משנים את מנוע האקורדים, כדאי לחזור על הבדיקה הזו.
